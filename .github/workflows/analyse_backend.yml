name: Analyse backend

on: [pull_request]

jobs:
  analyse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Prepare analysis
        run: |
          cd application/backend
          php bin/console cache:warmup --env=dev
      - name: Test
        run: |
          cd application/backend
          ls -l application/backend/
          ls -l application/backend/var/
          ls -l application/backend/var/cache/
          ls -l application/backend/var/cache/dev/
          ls -l application/backend/var/cache/dev/Symfony
      - name: ECS check
        run: make ecs-check
      - name: PHPStan
        run: make phpstan-analyse
